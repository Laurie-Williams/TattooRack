<h1><%= @user.username %></h1>

<% @user.pieces.all_by_created_at.each_with_index do |piece| %>
  <%= image_tag piece.image_url %>
  <h3>Title: <%= piece.title %></h3>
  <%= link_to "Show", piece_path(piece) %>
  <hr>
<% end %>